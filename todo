1. Add AuthInterceptor for DIO (not pass the API key)
2. Add Interceptor for adding header to DIO
3. Convert json to DTO and then convert to Entity (remote data source impl)
4. Make Http Provider cleaner
5. Fix WEBM support (now works for ANDROID, but need some improvements, for IOS is not working properly)
6. Add filtering options in UI and BLoC